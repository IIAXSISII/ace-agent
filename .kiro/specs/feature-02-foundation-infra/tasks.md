# Tasks: F02 — Foundation Infrastructure

> Deploys the AWS Cloud Engineering Agent to AWS for the first time.
> Builds on F01 (Core Orchestrator, complete). All CloudFormation templates must pass cfn-lint before deployment.
> Done when: `make deploy-foundation ENV=production && make deploy-platform ENV=production` succeeds and the Orchestrator endpoint responds to `/ping` within the VPC.

---

## Task List

- [x] 1. CloudFormation foundation tier — networking
  - [x] 1.1 Create `cloudformation/stacks/foundation/networking.yaml` with a single `AgentSecurityGroup` resource (HTTPS port 443 egress to `VpcCidr`, no inbound rules); parameters `Environment`, `Application`, `VpcId`, `SubnetIds` (CommaDelimitedList), `VpcCidr`; all resources tagged `Environment`, `Application`, `ManagedBy: CloudFormation`, `Stack: !Ref AWS::StackName`; no VPC endpoints or Gateway endpoints — those are provisioned manually
  - [x] 1.2 Add `Outputs` to `networking.yaml` exporting `VpcId`, `SubnetIds`, `AgentSecurityGroupId` with naming convention `${AWS::StackName}-<ResourceName>`; add corresponding `AWS::SSM::Parameter` resources publishing values under `/${Application}/${Environment}/networking/<resource>`
  - [x] 1.3 Run `cfn-lint cloudformation/stacks/foundation/networking.yaml` and fix all errors until exit code is 0

- [x] 2. CloudFormation foundation tier — identity
  - [x] 2.1 Create `cloudformation/stacks/foundation/identity.yaml` with `OrchestratorKmsKey` (KMS key with rotation enabled), `OrchestratorKmsKeyAlias`, `OrchestratorExecutionRole` (IAM Role with trust policy for `bedrock-agentcore.amazonaws.com` only; inline policy scoped exclusively to Orchestrator-required actions: `bedrock:InvokeModel`, `bedrock:ApplyGuardrail`, `dynamodb:GetItem/PutItem/Query/UpdateItem` on all 7 table ARNs, `s3:GetObject/PutObject/ListBucket` on all 3 bucket ARNs, `secretsmanager:GetSecretValue`, `xray:PutTraceSegments/PutTelemetryRecords`, `logs:CreateLogGroup/CreateLogStream/PutLogEvents`; all resource ARNs use `Fn::Sub` with `${AWS::AccountId}` and `${AWS::Region}` — no wildcard resources), `ExternalApiCredentialsSecret` (Secrets Manager secret encrypted with `OrchestratorKmsKey`); all resources tagged; `DeletionPolicy: Retain` and `UpdateReplacePolicy: Retain` on KMS key and secret; add a `Metadata` comment stating this stack owns only the Orchestrator role — sub-agent roles are provisioned in their own stacks
  - [x] 2.2 Add `Outputs` to `identity.yaml` exporting `OrchestratorExecutionRoleArn` and `ExternalApiCredentialsSecretArn` with naming convention `${AWS::StackName}-<ResourceName>`; add corresponding `AWS::SSM::Parameter` resources under `/${Application}/${Environment}/identity/<resource>`
  - [x] 2.3 Run `cfn-lint cloudformation/stacks/foundation/identity.yaml` and fix all errors until exit code is 0

- [x] 3. CloudFormation platform tier — guardrails
  - [x] 3.1 Create `cloudformation/stacks/platform/guardrails.yaml` with `AWS::Bedrock::Guardrail` resource; configure `SensitiveInformationPolicyConfig` with PII entities (NAME, EMAIL, PHONE, ADDRESS, AWS_ACCESS_KEY, AWS_SECRET_KEY, PASSWORD, USERNAME) all with action `ANONYMIZE`; configure `ContentPolicyConfig` with hate/violence/sexual/insults at HIGH filter strength for input and output; configure `ContextualGroundingPolicyConfig` with grounding threshold 0.75 and relevance threshold 0.75; configure `WordPolicyConfig` to block prompt injection patterns; all resources tagged
  - [x] 3.2 Add `Outputs` to `guardrails.yaml` exporting `GuardrailId`, `GuardrailArn`, `GuardrailVersion` with naming convention `${AWS::StackName}-<ResourceName>`; add corresponding `AWS::SSM::Parameter` resources publishing values under `/${Application}/${Environment}/guardrails/<resource>`
  - [x] 3.3 Run `cfn-lint cloudformation/stacks/platform/guardrails.yaml` and fix all errors until exit code is 0

- [x] 4. CloudFormation platform tier — memory
  - [x] 4.1 Create `cloudformation/stacks/platform/memory.yaml` with two `AWS::BedrockAgentCore::MemoryStore` resources: `ShortTermMemoryStore` (session-scoped) and `LongTermMemoryStore` (cross-session semantic retrieval); all resources tagged
  - [x] 4.2 Add `Outputs` to `memory.yaml` exporting `ShortTermMemoryStoreId` and `LongTermMemoryStoreId` with naming convention `${AWS::StackName}-<ResourceName>`; add corresponding `AWS::SSM::Parameter` resources publishing values under `/${Application}/${Environment}/memory/<resource>`
  - [x] 4.3 Run `cfn-lint cloudformation/stacks/platform/memory.yaml` and fix all errors until exit code is 0

- [x] 5. CloudFormation platform tier — gateway
  - [x] 5.1 Create `cloudformation/stacks/platform/gateway.yaml` with `AWS::BedrockAgentCore::Gateway` resource; configure OAuth2 authentication; all resources tagged
  - [x] 5.2 Add `Outputs` to `gateway.yaml` exporting `GatewayEndpointUrl` and `GatewayId` with naming convention `${AWS::StackName}-<ResourceName>`; add corresponding `AWS::SSM::Parameter` resources publishing values under `/${Application}/${Environment}/gateway/<resource>`
  - [x] 5.3 Run `cfn-lint cloudformation/stacks/platform/gateway.yaml` and fix all errors until exit code is 0

- [x] 6. CloudFormation module — agent-runtime-endpoint
  - [x] 6.1 Create `cloudformation/modules/agent-runtime-endpoint/module.yaml` as a reusable CloudFormation module; parameters: `AgentName`, `ContainerImageUri`, `ExecutionRoleArn` (required — the calling stack must provision its own dedicated role and pass the ARN here; the module SHALL NOT create any IAM role), `VpcId`, `SubnetIds`, `SecurityGroupId`, `GatewayEndpointUrl`, `Environment`, `Application`, `EnvironmentVariables`; resources: `AWS::BedrockAgentCore::AgentRuntimeEndpoint` (uses `ExecutionRoleArn` parameter directly) and `AWS::BedrockAgentCore::GatewayTarget` (Gateway registration); outputs: `RuntimeEndpointArn`, `RuntimeEndpointUrl`; all resources tagged; add a `Metadata` comment stating that IAM role creation is intentionally excluded — each agent stack owns its own role
  - [x] 6.2 Run `cfn-lint cloudformation/modules/agent-runtime-endpoint/module.yaml` and fix all errors until exit code is 0

- [x] 7. CloudFormation application tier — orchestrator
  - [x] 7.1 Create `cloudformation/stacks/application/agents/orchestrator.yaml`; consume `VpcId`, `SubnetIds`, `AgentSecurityGroupId` via `Fn::ImportValue` from networking stack; consume `OrchestratorExecutionRoleArn` via `Fn::ImportValue` from identity stack (this is the Orchestrator's dedicated role — it must not be reused by any sub-agent stack); provision `AWS::BedrockAgentCore::AgentRuntimeEndpoint` with `ExecutionRoleArn` set to the imported Orchestrator role ARN; set env vars `BEDROCK_GUARDRAIL_ID`, `AGENTCORE_MEMORY_STORE_ID`, `AGENTCORE_LONG_TERM_MEMORY_STORE_ID`, `AGENTCORE_GATEWAY_URL`, `BEDROCK_MODEL_ID`, `DISABLE_ADOT_OBSERVABILITY=true`, `OTEL_STACK=production`; consume `GuardrailId`, `ShortTermMemoryStoreId`, `LongTermMemoryStoreId`, `GatewayEndpointUrl` via `Fn::ImportValue` from their respective stacks; parameters: `Environment`, `Application`, `OrchestratorImageUri`, `BedrockModelId`, `NetworkingStackName`, `IdentityStackName`, `GuardrailsStackName`, `MemoryStackName`, `GatewayStackName`; all resources tagged
  - [x] 7.2 Add `Outputs` to `orchestrator.yaml` exporting `RuntimeEndpointArn` and `RuntimeEndpointUrl` with naming convention `${AWS::StackName}-<ResourceName>`; add corresponding `AWS::SSM::Parameter` resources under `/${Application}/${Environment}/agents/orchestrator/<resource>`
  - [x] 7.3 Run `cfn-lint cloudformation/stacks/application/agents/orchestrator.yaml` and fix all errors until exit code is 0

- [x] 8. Deployment scripts
  - [x] 8.1 Create `cloudformation/scripts/deploy.sh` — accepts `ENV` as first argument; runs `make deploy-foundation`, `make deploy-platform`, `make deploy-agents` in sequence; uses `set -euo pipefail`; passes `ENV`, `REGION`, `PROFILE` through to make targets
  - [x] 8.2 Create `cloudformation/scripts/update-agent.sh` — accepts `AGENT` as first argument and `ENV` as second; runs `make deploy-agent AGENT=$AGENT ENV=$ENV`; uses `set -euo pipefail`; validates that `AGENT` argument is provided
  - [x] 8.3 Update `cloudformation/Makefile` `deploy-foundation` target to deploy stacks in order: networking → identity → data → storage (networking and identity are new; data and storage already existed)
  - [x] 8.4 Update `cloudformation/Makefile` `deploy-platform` target to deploy only F02 platform stacks in order: guardrails → memory → gateway (knowledge and observability are deferred to F03/F05 and must not be included in F02's deploy-platform target)

- [x] 9. Parameter file updates
  - [x] 9.1 Update `cloudformation/parameters/production.json` to add new parameter entries for F02: `VpcId` (`<REPLACE_WITH_VPC_ID>`), `SubnetIds` (`<REPLACE_WITH_SUBNET_IDS>`), `VpcCidr` (`<REPLACE_WITH_VPC_CIDR>`), `BedrockModelId` (`anthropic.claude-3-5-sonnet-20241022-v2:0`), `OrchestratorImageUri` (`<REPLACE_WITH_ECR_IMAGE_URI>`), `NetworkingStackName` (`ace-agent-foundation-networking-production`), `IdentityStackName` (`ace-agent-foundation-identity-production`), `GuardrailsStackName` (`ace-agent-platform-guardrails-production`), `MemoryStackName` (`ace-agent-platform-memory-production`), `GatewayStackName` (`ace-agent-platform-gateway-production`)
  - [x] 9.2 Update `cloudformation/parameters/local.json` to add the same parameter keys with local-appropriate placeholder values so `make deploy-foundation ENV=local` does not fail on missing parameters

- [x] 10. Guardrails Python wrapper
  - [x] 10.1 Implement `src/guardrails/bedrock.py` with `get_guardrailed_llm(model_id=None) -> ChatBedrock` factory: reads `BEDROCK_GUARDRAIL_ID` env var; when set, constructs `ChatBedrock` with `guardrails={"guardrailIdentifier": ..., "guardrailVersion": ..., "trace": "enabled"}`; when unset, constructs plain `ChatBedrock`; reads `BEDROCK_MODEL_ID` env var for model selection with fallback to `anthropic.claude-3-5-sonnet-20241022-v2:0`
  - [x] 10.2 Implement `log_violation(violation_type, severity, redacted_content, request_id, policy_version="DRAFT") -> None` in `src/guardrails/bedrock.py`: generates UUID `violationId`; computes `ttl` as current Unix epoch + 90 days; writes item to `guardrail-violations` table via `src/storage/dynamodb.py` `put_item` helper; uses `datetime.now(timezone.utc).isoformat()` for timestamp
  - [x] 10.3 Update `src/orchestrator/nodes/scan_output_guardrails.py` to import and use `get_guardrailed_llm()` from `src/guardrails/bedrock.py` instead of instantiating `ChatBedrock` directly; call `log_violation()` with `violationType="CONTENT"` when guardrail blocks an empty response and `violationType="INJECTION"` when a guardrail exception is caught; pass `state.get("session_id", "unknown")` as `request_id`

- [x] 11. AgentCore Memory wiring verification
  - [x] 11.1 Read `src/orchestrator/memory.py` and confirm `get_checkpointer()` satisfies both branches: (a) when `AGENTCORE_MEMORY_STORE_ID` env var is set, it returns `AgentCoreMemorySaver` initialised with that store ID; (b) when unset, it returns `MemorySaver()`; if either branch is missing or incorrect, update the implementation to match — this is a verification + fix task, not a no-op

- [x] 12. ARM64 container build
  - [x] 12.1 Create `Dockerfile` at repo root with multi-stage ARM64 build: builder stage uses `FROM --platform=linux/arm64 python:3.12-slim`, copies `pyproject.toml`, runs `pip install .`; runtime stage uses `FROM --platform=linux/arm64 python:3.12-slim`, copies site-packages and binaries from builder, copies `src/` directory; exposes port 8080; sets `CMD ["uvicorn", "src.orchestrator.main:app", "--host", "0.0.0.0", "--port", "8080"]`

- [x] 13. Unit tests — guardrails wrapper
  - [x] 13.1 Write `tests/unit/test_guardrails_bedrock.py` with example-based unit tests: `test_passthrough_when_guardrail_id_unset` — when `BEDROCK_GUARDRAIL_ID` is not set, `get_guardrailed_llm()` returns a `ChatBedrock` instance with no `guardrails` attribute set; `test_guardrail_config_when_id_set` — when `BEDROCK_GUARDRAIL_ID=test-id`, `get_guardrailed_llm()` returns a `ChatBedrock` with `guardrails["guardrailIdentifier"] == "test-id"`; `test_log_violation_writes_all_fields` — `log_violation(...)` calls `put_item` exactly once with an item containing all required fields (`violationId`, `violationType`, `severity`, `redactedContent`, `requestId`, `policyVersion`, `timestamp`, `ttl`); `test_log_violation_ttl_is_90_days` — the `ttl` field is within 1 second of `int(time.time()) + 90*24*60*60`
  - [x] 13.2 Write `tests/unit/test_memory_wiring.py` with example-based unit tests: `test_memory_saver_when_env_unset` — when `AGENTCORE_MEMORY_STORE_ID` is not set, `get_checkpointer()` returns a `MemorySaver` instance; `test_agentcore_memory_saver_when_env_set` — when `AGENTCORE_MEMORY_STORE_ID=store-123`, `get_checkpointer()` returns an `AgentCoreMemorySaver` instance

- [x] 14. Property-based tests — guardrails correctness properties
  - [x] 14.1 Write property-based tests in `tests/unit/test_pbt_guardrails.py` using `hypothesis` for Property 30 (PII Redaction Completeness): define `hypothesis` strategy `pii_containing_text()` that generates strings with injected PII patterns (names matching `[A-Z][a-z]+ [A-Z][a-z]+`, emails matching `\w+@\w+\.\w+`, API key patterns matching `AKIA[A-Z0-9]{16}`); mock `ChatBedrock.invoke` to return a response with PII replaced by typed placeholders; assert that the returned `final_response` does not contain the original PII values; tag test `# Feature: feature-02-foundation-infra, Property 30: PII Redaction Completeness`
  - [x] 14.2 Write property-based tests in `tests/unit/test_pbt_guardrails.py` for Property 31 (Prompt Injection Blocking): define `hypothesis` strategy `injection_pattern_text()` that generates strings containing injection patterns (drawn from a pool: "ignore previous instructions", "you are now", "disregard all prior", "jailbreak", "DAN mode"); mock `ChatBedrock.invoke` to raise a guardrail intervention exception; mock `put_item` to capture calls; assert that `scan_output_guardrails` returns state with `final_response == _GUARDRAIL_BLOCKED_RESPONSE` and that `put_item` was called with `violationType="INJECTION"`; tag test `# Feature: feature-02-foundation-infra, Property 31: Prompt Injection Blocking`
  - [x] 14.3 Write property-based tests in `tests/unit/test_pbt_guardrails.py` for Property 32 (Guardrail Violation Log Completeness): define `hypothesis` strategy `violation_event()` that generates dicts with `violation_type` drawn from `["PII", "INJECTION", "CONTENT", "GROUNDING"]`, `severity` drawn from `["HIGH", "MEDIUM", "LOW"]`, `redacted_content` as arbitrary text (max 500 chars), `request_id` as UUID string; mock `put_item` to capture calls; call `log_violation(**violation)`; assert exactly one `put_item` call was made; assert the captured item contains all required fields with correct types; assert `ttl` is greater than `int(time.time())`; tag test `# Feature: feature-02-foundation-infra, Property 32: Guardrail Violation Log Completeness`

- [x] 15. CloudFormation template validation tests
  - [x] 15.1 Write `tests/unit/test_cfn_lint.py` with integration tests that invoke `cfn-lint` as a subprocess on each new template and assert exit code 0: `networking.yaml`, `identity.yaml`, `guardrails.yaml`, `memory.yaml`, `gateway.yaml`, `orchestrator.yaml`, `agent-runtime-endpoint/module.yaml`; each test is independent and reports the cfn-lint stderr output on failure
